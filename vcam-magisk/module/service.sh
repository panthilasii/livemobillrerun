#!/system/bin/sh
# Runs after boot completes. We don't need anything here yet —
# the YUV path is already set up by post-fs-data.sh, and the
# Android receiver app starts itself via the user's launcher.
exit 0
